@using System.Data
@model System.Data.DataTable

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">

<h2 class="my-4 text-primary fw-bold ">🏢 Department List</h2>

@if (TempData["Message"] != null)
{
    <div class="alert alert-success text-center fw-semibold">@TempData["Message"]</div>
}

<div class="table-responsive shadow-sm rounded-4 border">
    <table class="table table-bordered table-hover align-middle text-center mb-0">
        <thead class="table-primary text-white align-middle">
            <tr>
                <th>Department ID <br><i class="bi bi-hash fs-5 text-warning"></i></th>
                <th>Department Name <br><i class="bi bi-building fs-5 text-info"></i></th>
                <th>Description <br><i class="bi bi-card-text fs-5 text-secondary"></i></th>
                <th>Is Active <br><i class="bi bi-toggle-on fs-5 text-success"></i></th>
                <th>Created <br><i class="bi bi-calendar-plus fs-5 text-dark"></i></th>
                <th>Modified <br><i class="bi bi-pencil-square fs-5 text-warning"></i></th>
                <th>User ID <br><i class="bi bi-person-circle fs-5 text-primary"></i></th>
                <th class="col-2">Action <br><i class="bi bi-tools fs-5 text-danger"></i></th>
            </tr>
        </thead>
        <tbody class="table-group-divider">
            @foreach (DataRow dr in Model.Rows)
            {
                <tr>
                    <td><span class="text-dark badge bg-primary-light rounded-circle p-2 px-3">@dr["DepartmentID"]</span></td>
                    <td>@dr["DepartmentName"]</td>
                    <td>@dr["Description"]</td>
                    <td>
                        <span class="badge bg-@((dr["IsActive"].ToString() == "True") ? "success" : "secondary")">
                            @((dr["IsActive"].ToString() == "True") ? "Active" : "Inactive")
                        </span>
                    </td>
                    <td>@Convert.ToDateTime(dr["Created"]).ToString("dd MMM yyyy")</td>
                    <td>@Convert.ToDateTime(dr["Modified"]).ToString("dd MMM yyyy")</td>
                    <td>@dr["UserID"]</td>
                    <td>
                        <div class="d-flex justify-content-center gap-2">
                            <a class="btn btn-sm btn-outline-success"
                               asp-controller="Department"
                               asp-action="DepartmentEdit"
                               asp-route-DepartmentID="@dr["DepartmentID"]"
                               title="Edit">
                                <i class="bi bi-pencil-fill"></i>
                                Edit
                            </a>

                            <a class="btn btn-sm btn-outline-danger"
                               asp-controller="Department"
                               asp-action="DepartmentDelete"
                               asp-route-DepartmentID="@dr["DepartmentID"]"
                               onclick="return confirm('Are you sure you want to delete this department?');"
                               title="Delete">
                                <i class="bi bi-trash-fill"></i>
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
